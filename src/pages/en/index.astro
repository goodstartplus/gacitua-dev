---
import { Image } from 'astro:assets';
import ActionLink from '../../components/astro/ActionLink.astro';
import ProjectCard from '../../components/astro/ProjectCard.astro';
import SectionHeading from '../../components/astro/SectionHeading.astro';
import ProjectPreviewModal from '../../components/react/ProjectPreviewModal';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { ui } from '../../utils/i18n';
import { getFeaturedProjects } from '../../utils/projects';

const featuredProjects = await getFeaturedProjects('en');
const home = ui.en.home;
const stats = [
  { value: '05', label: home.statLabels[0] },
  { value: '02', label: home.statLabels[1] },
  { value: '06', label: home.statLabels[2] },
];

const highlightItems = home.highlightItems;
const profileMeta = home.profileMeta;
const profileActions = [
  { href: '/en/projects/', label: home.profileActions[0] },
  { href: '/en/about', label: home.profileActions[1] },
  { href: '/en/stack', label: home.profileActions[2] },
  { href: '/en/contact', label: home.profileActions[3] },
];

const modalProjects = featuredProjects.map((project) => ({
  title: project.data.title,
  slug: project.data.slug,
  type: project.data.type,
  status: project.data.status,
  summary: project.data.summary,
  shortDescription: project.data.shortDescription,
  stack: project.data.stack,
  tags: project.data.tags,
  coverImage: project.data.coverImage,
  externalUrl: project.data.externalUrl,
  year: project.data.year,
  clientOrPersonal: project.data.clientOrPersonal,
}));
---

<BaseLayout
  lang="en"
  title="André Gacitua — Builder Profile and Projects"
  description="Bilingual homepage for André Gacitua, presenting real digital products, systems, websites, automations, and a profile-first project archive."
>
  <section class="page-container section-spacing px-6 pt-12 md:pt-20">
    <div class="surface-panel rounded-[2rem] p-6 md:p-10">
      <div class="grid gap-10 lg:grid-cols-[0.34fr_1fr_0.7fr] lg:items-end">
        <div class="flex flex-col items-start gap-4">
          <div class="profile-avatar overflow-hidden">
            <Image
              src="/images/profile/andre-gacitua.jpeg"
              alt="André Gacitua"
              width={1200}
              height={1200}
              class="h-full w-full object-cover"
            />
          </div>
          <div class="flex flex-wrap gap-2">
            {profileMeta.map((item) => (
              <span class="pill">{item}</span>
            ))}
          </div>
        </div>

        <div>
          <p class="eyebrow">{home.heroEyebrow}</p>
          <h1 class="display-title mt-5 max-w-4xl text-white">{home.heroTitle}</h1>
          <p class="body-large mt-6 max-w-2xl">{home.heroDescription}</p>

          <div class="mt-8 flex flex-wrap gap-3">
            <ActionLink href="/en/projects/" variant="primary" label={home.heroPrimary} />
            <ActionLink href="/en/about" variant="secondary" label={home.heroSecondary} />
          </div>
        </div>

        <div class="grid gap-4 sm:grid-cols-3 lg:grid-cols-1">
          {stats.map((stat) => (
            <div class="stat-card">
              <span class="stat-value">{stat.value}</span>
              <span class="stat-label">{stat.label}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section id="profile-highlights" class="page-container px-6 pb-6">
    <div class="surface-panel rounded-[2rem] p-6 md:p-8">
      <div class="mb-6 flex items-end justify-between gap-4">
        <SectionHeading
          eyebrow={home.highlightEyebrow}
          title={home.highlightTitle}
          description={home.highlightDescription}
        />
      </div>

      <div class="flex gap-4 overflow-x-auto pb-2">
        {highlightItems.map((item, index) => (
          <a href={index === 0 ? '/en/projects/' : index === 1 ? '/en/projects/' : index === 2 ? '/en/projects/' : index === 3 ? '/en/projects/' : '/en/about'} class="highlight-chip">
            <div class="highlight-orb">{item.slice(0, 2).toUpperCase()}</div>
            <span class="text-sm text-neutral-300">{item}</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <section id="design-foundation" class="page-container section-spacing px-6 pb-6">
    <SectionHeading
      eyebrow={home.foundationEyebrow}
      title={home.foundationTitle}
      description={home.foundationDescription}
    />

    <div class="mt-8 flex flex-wrap gap-3">
      {highlightItems.map((item) => (
        <span class="pill">{item}</span>
      ))}
    </div>

    <div class="mt-8 grid gap-4 lg:grid-cols-3">
      <div class="surface-panel rounded-[1.75rem] p-6 lg:col-span-2">
        <p class="eyebrow">{home.visualIntent}</p>
        <p class="body-copy mt-4 max-w-2xl">{home.visualIntentCopy}</p>
      </div>
      <div class="surface-panel rounded-[1.75rem] p-6">
        <p class="eyebrow">{home.responsiveRule}</p>
        <p class="body-copy mt-4">{home.responsiveRuleCopy}</p>
      </div>
    </div>
  </section>

  <section id="featured-projects" class="page-container section-spacing px-6 pt-6">
    <div class="mb-8 flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
      <SectionHeading
        eyebrow={home.featuredEyebrow}
        title={home.featuredTitle}
        description={home.featuredDescription}
      />
      <span class="muted-label text-sm">{featuredProjects.length} {home.featuredCountSuffix}</span>
    </div>

    <div class="grid-instagram">
      {featuredProjects.map((project) => <ProjectCard project={project} modalEnabled />)}
    </div>
  </section>

  <ProjectPreviewModal client:load projects={modalProjects} locale="en" />

  <section id="about-builder" class="page-container section-spacing px-6 pb-6">
    <div class="grid gap-4 lg:grid-cols-[1.1fr_0.9fr]">
      <div class="surface-panel rounded-[2rem] p-6 md:p-8">
        <SectionHeading
          eyebrow={home.aboutEyebrow}
          title={home.aboutTitle}
          description={home.aboutDescription}
        />
      </div>
      <div class="surface-panel rounded-[2rem] p-6 md:p-8">
        <p class="eyebrow">{home.profileActionsTitle}</p>
        <div class="mt-5 flex flex-wrap gap-3">
          {profileActions.map((action) => (
            <ActionLink href={action.href} variant="secondary" label={action.label} />
          ))}
        </div>
      </div>
    </div>
  </section>

  <section id="stack-overview" class="page-container px-6 pb-6">
    <div class="surface-panel rounded-[2rem] p-6 md:p-8">
      <div class="mb-6 flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
        <SectionHeading
          eyebrow={home.stackEyebrow}
          title={home.stackTitle}
          description={home.stackDescription}
        />
      </div>
      <div class="flex flex-wrap gap-3">
        {['Astro', 'TypeScript', 'Cloudflare', 'React', 'Firebase', 'WordPress', 'Node.js'].map((item) => (
          <span class="pill">{item}</span>
        ))}
      </div>
    </div>
  </section>

  <section id="contact-entry" class="page-container section-spacing px-6 pt-4">
    <div class="surface-panel rounded-[2rem] p-6 md:p-8">
      <div class="grid gap-6 md:grid-cols-[1fr_auto] md:items-end">
        <SectionHeading
          eyebrow={home.contactEyebrow}
          title={home.contactTitle}
          description={home.contactDescription}
        />
        <ActionLink href="mailto:hello@example.com" variant="primary" label="hello@example.com" />
      </div>
    </div>
  </section>
</BaseLayout>